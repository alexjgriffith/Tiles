1. Check incoming {id,token} pair
   a. if id=0 show enter name and password screen
   b. id_to_token(id)!=to system error screen + enter name/pword
   c. Otherwise rout message

2. From Name and Password Screen
   a. Get Name
   b. Find Match
   c. Send Map
   d. On dl confirmation start game sync

3. Message Routing
   a. get_match(id) -> returns a match index
   b. sends updates to all members who are subsrcribed to this match



State Machine?

process states

NoId -> NoMatch
NoMatch -> NoId | LookingForMatch
LookingForMatch -> NoId | HasMatch
HasMatch -> NoId | NoMatch
